import 'package:flutter/material.dart';

/** Seccion de mis blogs 2  seleccionada por defecto */
class myBLogs extends StatelessWidget {
  const myBLogs({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Mis blogs"),
    );
  }
}
